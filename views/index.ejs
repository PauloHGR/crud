<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8"/>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Clientes já cadastrados no sistema.</p>
    <table style="width: 50%">

      <tr style="background-color:lightgray">
        <td><b>Nome</b></td>
        <td><b>Idade</b></td>
        <td><b>UF</b></td>
        <td><b>Ações</b></td>
      </tr>

      <% if(!docs || docs.length == 0) { %>
        <tr>
          <td><p>Nenhum cliente Cadastrado</p></td>
        </tr>
      <%} else {
        docs.forEach(function(customer){ %>
          <tr>
            <td style="width: 50%"><%= customer.nome %></td>
            <td style="width: 15%"><%= customer.idade %></td>
            <td style="width: 15%"><%= customer.uf %></td>
            <td><a href="/new/edit/<%= customer._id %>">Editar</a></td>
          </tr>
        <%})
      }%>
    </table>

    <a href="/new">Cadastrar Novo</a>

  </body>
</html>
